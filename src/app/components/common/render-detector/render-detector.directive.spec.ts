import { RenderDetectorDirective } from './render-detector.directive';

describe('RenderDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
