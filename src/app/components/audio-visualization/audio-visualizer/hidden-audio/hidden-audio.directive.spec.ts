import { HiddenAudioDirective } from './hidden-audio.directive';

describe('HiddenAudioDirective', () => {
  it('should create an instance', () => {
    const directive = new HiddenAudioDirective();
    expect(directive).toBeTruthy();
  });
});
